// const hugTree = (hugs, trees) => hugs.map((hug) => {
//   const findHug = hug;
//   const tree = trees.find(t => t.id === findHug.treeId);
//   if (tree) {
//     findHug.treeName = tree.name;
//   }
//   return findHug;
// });

// const hugSquirrel = (hugs, squirrels) => hugs.map((hug) => {
//   const findHug = hug;
//   const squirrel = squirrels.find(s => s.id === findHug.squirrelId);
//   if (squirrel) {
//     findHug.squirrelName = squirrel.name;
//   }
//   return findHug;
// });

// export default { hugTree, hugSquirrel };
